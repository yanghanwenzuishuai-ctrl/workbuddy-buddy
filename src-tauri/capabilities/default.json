{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Allow the pet window to receive backend state events.",
  "windows": ["pet"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
